/**
 * Administration IHM (Qute) and the earn Simulator — imfid's only human interface.
 *
 * <p>Conforms to {@code docs/guide-ihm-administration.pdf} (§21) and follows the
 * admin decomposition of §23: rules driven by the shared schema registry (§12),
 * communities, accounts, and the Programme entry for settings (§25.1). The
 * Simulator replays the {@code /earn} projection with no side effect, unlike a
 * real demo pass which consumes the card balance.</p>
 */
package com.intermarche.fidelity.ui;
